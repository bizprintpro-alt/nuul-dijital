{
  "buildCommand": "pnpm --filter @nuul/db exec prisma generate && pnpm --filter @nuul/web build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
