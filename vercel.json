{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/db && pnpm exec prisma generate && cd ../../apps/web && pnpm exec next build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
